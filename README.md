# OS-Base
Simple OS that boots from a bootloader to the kernel
Fully in x86 assembly.
-----------------------------------------------------------------
You need to install qemu and nasm before running "build_OS.bat"
